

<h2>Junit是自动化的测试，手动的输出会导致一直阻塞，也就是说junit不支持手动输入，否则会导致当前线程一直阻塞，转圈圈，所以test类不要用scanner那种控制台手动输入数据的方式，把数据直接写成形参，测试的时候直接写成参数测试吧；
JUnit总是为每个@Test方法创建一个测试类实例，所以必须要声明成public，也就是公共的，才能让junit给你创建，否则权限不够的.</h2>